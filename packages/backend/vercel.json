{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nestjs",
  "buildCommand": "npm run prisma:generate && npx prisma migrate deploy && npm run bootstrap:deploy && npm run build",
  "functions": {
    "api/[...path].ts": {
      "includeFiles": "node_modules/.prisma/client/**"
    }
  }
}
